# max

## 说明

检查第一个数字是否小于或等于第二个数字

## 返回值

- 类型: `unknown`
- 说明: 如果 num <= max 则返回 `true`，否则为 `false`

## 示例

```ts
import { max } from 'arcdash'

max(5, 10) // true
max(10, 10) // true
max(15, 10) // false
```

