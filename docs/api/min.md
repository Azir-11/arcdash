# min

## 说明

检查第一个数字是否大于或等于第二个数字

## 返回值

- 类型: `unknown`
- 说明: 如果 num >= min 则返回 `true`，否则为 `false`

## 示例

```ts
import { min } from 'arcdash'

min(10, 5) // true
min(5, 5) // true
min(3, 5) // false
```

